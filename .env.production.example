# ===========================================
# Production Environment Variables
# ===========================================
# Copy this file to .env.production and fill in the values

# Google Gemini API (Required)
GOOGLE_API_KEY=your_google_api_key_here

# CORS Settings
# Comma-separated list of allowed origins
# Example: http://your-domain.com,https://your-domain.com
ALLOWED_ORIGINS=http://your-server-ip

# VOICEVOX (Set by docker-compose, usually no change needed)
VOICEVOX_HOST=http://voicevox:50021

# Redis (Set by docker-compose, usually no change needed)
REDIS_URL=redis://redis:6379/0

# Supabase (Optional - for future features)
# SUPABASE_URL=your_supabase_url
# SUPABASE_KEY=your_supabase_key

# ===========================================
# Frontend Build Variables (for Dockerfile.prod)
# ===========================================
# These are passed as build args in docker-compose.prod.yml

# API URL (accessible from browser)
# For single server setup, use empty string (nginx proxies to backend)
VITE_API_URL=
VITE_WS_URL=
